{
  "name": "chantier-manager",
  "version": "2.0.0",
  "private": true,
  "description": "Application de gestion de chantiers (PWA) — API Node/Express + PostgreSQL",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0"
  },
  "engines": {
    "node": ">=18"
  }
}
